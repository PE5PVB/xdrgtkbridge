// Serial Bridge plugin has no frontend UI; it exposes a raw TCP
// passthrough (default port 7373) to the tuner's serial/xdrd stream.
console.log('Serial Bridge plugin loaded (TCP passthrough on server side).');
